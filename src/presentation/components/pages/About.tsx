import { h } from "preact"

export default function About() {
  return (
    <div style={{ padding: "1rem" }}>
      <h1>About</h1>
      <p>This is a sample Weather app with Preact + Clean Arch</p>
    </div>
  )
}